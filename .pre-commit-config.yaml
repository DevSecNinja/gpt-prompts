# See https://pre-commit.com for more information
# To install: pip install pre-commit && pre-commit install
repos:
  - repo: local
    hooks:
      - id: validate-prompts
        name: Validate prompt structure
        entry: bash tests/validate-prompts.sh
        language: system
        pass_filenames: false
        always_run: true
        stages: [commit]
